{
  "name": "app",
  "version": "1.0.0",
  "scripts": {
    "serve": "browser-sync start --server --port 8000 --files '**/*.html' --logLevel silent",
    "clean": "rimraf dist",
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "test": "jest --watch"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "jest": "^19.0.2",
    "rimraf": "^2.5.4",
    "yarn": "^0.16.1"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "next": "^2.2.0"
  }
}
