html, body, img, video { width: 100%; }

body { box-sizing: border-box; }

h1, h2, h3, h4, h5, h6, ul {
    margin: 0;
    padding: 0;
}

a, a:link, a:visited, a:hover, a:active { text-decoration: none; }
