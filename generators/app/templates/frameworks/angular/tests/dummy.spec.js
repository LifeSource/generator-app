describe("Dummy Spec", function () {


    it("should be able to run tests in browser using browser-sync", function () {

        expect(true).to.be.true;
        expect(false).to.be.true;
    });

    it("1 + 1 should = 2", function () {
        expect(1+2).to.equal(3);
    });

});
